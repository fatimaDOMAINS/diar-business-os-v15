function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
async function ask(){const d=await (await fetch('/api/advisor',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({query:q.value})})).json();result.innerHTML=`<p>${esc(d.summary)}</p>${(d.matches||[]).map(p=>`<div class="provider"><b>${esc(p.name)}</b>${p.featured?'<span class="badge">Featured</span>':''}<br><small>${esc(p.provider_type)} · ${esc(p.country||'')} / ${esc(p.city||'')}</small><p>${esc(p.services||'')}</p><small>${p.verified?'Verified data':'Demo / unverified'} · ${esc(p.commercial_model||'')}</small></div>`).join('')}`;if(d.intent.project_type)project.value=d.intent.project_type}
async function compare(){const u=new URLSearchParams();if(ptype.value)u.set('type',ptype.value);if(countryfilter.value)u.set('country',countryfilter.value);const d=await (await fetch('/api/compare?'+u)).json();providers.innerHTML=(d.providers||[]).map(p=>`<div class="provider"><b>${esc(p.name)}</b>${p.featured?'<span class="badge">Featured</span>':''}<br><small>${esc(p.provider_type)} · ${esc(p.country||'')} / ${esc(p.city||'')}</small><p>${esc(p.services||'')}</p><small>${p.storage_capability?'Storage capable · ':''}${p.verified?'Verified provider data':'Demo / unverified until provider confirmation'}</small></div>`).join('')||'<p>No providers match.</p>'}
async function lead(){const u=new URL(location.href);
const body={name:name.value,company:company.value,contact:contact.value,buyer_type:buyer.value,project_type:project.value,country:country.value,city:city.value,property_type:property.value,roof_or_site_text:site.value,estimated_consumption_text:consumption.value,budget_text:budget.value,storage_required:storage.value==='1',timeline:timeline.value,notes:notes.value,
session_id:localStorage.getItem('solarenergy24_visitor_session')||'',source:u.searchParams.get('utm_source')||document.referrer||'direct',
utm_source:u.searchParams.get('utm_source'),utm_medium:u.searchParams.get('utm_medium'),utm_campaign:u.searchParams.get('utm_campaign'),landing_path:location.pathname,site_data_ready:sitedata.value==='1',consumption_data_ready:consumptiondata.value==='1',
grid_status_ready:gridstatus.value==='1',financing_context_ready:financecontext.value==='1',preferred_contact:contactpref.value};const d=await (await fetch('/api/lead',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)})).json();leadmsg.textContent=d.ok?'Project brief received. A relevant solar provider can now review the opportunity.':(d.error||'Unable to submit.')}
compare();